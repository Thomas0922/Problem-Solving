# LeetCode 61. Rotate List - 完整學習筆記

## 📋 題目描述

給你一個鏈表的頭節點 `head`，**旋轉鏈表**，將鏈表每個節點向右移動 `k` 個位置。

**範例**：
```
範例 1：
輸入：head = [1,2,3,4,5], k = 2
輸出：[4,5,1,2,3]

解釋：
  原始：1 → 2 → 3 → 4 → 5
  
  旋轉 1 次：5 → 1 → 2 → 3 → 4
  旋轉 2 次：4 → 5 → 1 → 2 → 3 ✓

範例 2：
輸入：head = [0,1,2], k = 4
輸出：[2,0,1]

解釋：
  原始：0 → 1 → 2
  
  旋轉 1 次：2 → 0 → 1
  旋轉 2 次：1 → 2 → 0
  旋轉 3 次：0 → 1 → 2 (回到原始)
  旋轉 4 次：2 → 0 → 1 ✓
  
  k=4 等同於 k=1 (4 % 3 = 1)
```

**限制**：
- 鏈表中節點的數目在範圍 `[0, 500]` 內
- `-100 <= Node.val <= 100`
- `0 <= k <= 2 * 10^9`

**關鍵理解**：
```
旋轉鏈表 = 把尾部節點移到頭部

右移 k 次：
  把最後 k 個節點移到前面

範例：[1,2,3,4,5], k=2
  
  原始：1 → 2 → 3 → 4 → 5
           └─────┘ └───┘
           保持    移到前面
           
  結果：4 → 5 → 1 → 2 → 3
        └───┘   └─────┘
        後k個   前n-k個
```

---

## 💡 基礎知識：鏈表旋轉

### 什麼是旋轉？

```
數組旋轉：
  [1,2,3,4,5] 右移 2 位
  → [4,5,1,2,3]

鏈表旋轉：
  1 → 2 → 3 → 4 → 5
  
  右移 2 位（移動最後 2 個到前面）
  → 4 → 5 → 1 → 2 → 3

本質：
  把尾部的節點移到頭部
```

### 旋轉的數學性質

```
重要性質：旋轉 n 次 = 不旋轉

範例：長度為 5 的鏈表
  旋轉 0 次 = 原樣
  旋轉 5 次 = 原樣（回到起點）
  旋轉 7 次 = 旋轉 2 次（7 % 5 = 2）

因此：k = k % n
  避免多餘的旋轉
```

### 旋轉的步驟

```
要右移 k 次：

方法 1：模擬（你的方法）
  重複 k 次：把最後一個節點移到前面
  
方法 2：一次性（最優）
  1. 找到新的尾節點（第 n-k 個節點）
  2. 斷開並重新連接
```

---

## 🔧 方法一：逐次旋轉（你的解法）

### 思路

```
每次旋轉 = 把最後一個節點移到前面

重複 k 次：
  1. 找到倒數第二個節點
  2. 把最後節點移到頭部
  3. 更新 head
```

### 你的代碼

```c
struct ListNode* rotateRight(struct ListNode* head, int k) {
    // 邊界處理
    if(head == NULL) return NULL;
    if(head->next == NULL) return head;
    
    // 計算鏈表長度
    struct ListNode* list = head;
    int n = 0;
    while(list){
        list = list->next;
        n++;
    }
    
    // 優化：k % n
    unsigned int count = k % n;
    
    // 旋轉 count 次
    while(count--){
        struct ListNode* start = head;
        
        // 找到倒數第二個節點
        while(start->next && start->next->next){
            start = start->next;
        }
        
        // 把最後節點移到頭部
        struct ListNode* last = start->next;
        last->next = head;
        head = last;
        start->next = NULL;
    }
    
    return head;
}
```

### 執行流程

```
範例：[1,2,3,4,5], k = 2

═══════════════════════════════════════
步驟 1：計算長度

  n = 5

═══════════════════════════════════════
步驟 2：優化 k

  count = k % n = 2 % 5 = 2
  需要旋轉 2 次

═══════════════════════════════════════
第 1 次旋轉：

  原始：1 → 2 → 3 → 4 → 5

  找倒數第二個節點：
    start 遍歷到 4
    
  操作：
    last = 5
    last.next = head  → 5.next = 1
    head = last       → head = 5
    start.next = NULL → 4.next = NULL
    
  結果：5 → 1 → 2 → 3 → 4

═══════════════════════════════════════
第 2 次旋轉：

  當前：5 → 1 → 2 → 3 → 4

  找倒數第二個節點：
    start 遍歷到 3
    
  操作：
    last = 4
    last.next = head  → 4.next = 5
    head = last       → head = 4
    start.next = NULL → 3.next = NULL
    
  結果：4 → 5 → 1 → 2 → 3 ✓

═══════════════════════════════════════
返回：head = 4 → 5 → 1 → 2 → 3
```

### 複雜度分析

| 項目 | 複雜度 | 說明 |
|------|--------|------|
| **時間複雜度** | O(n * k) | 每次旋轉遍歷 O(n)，旋轉 k 次 |
| **優化後時間** | O(n * (k%n)) | k % n 避免多餘旋轉 |
| **最壞情況** | O(n²) | 當 k = n-1 時 |
| **空間複雜度** | O(1) | 只用常數個指針 |

### 你的解法優點

**優勢**：
- ✅ **邏輯清晰**：模擬真實的旋轉過程
- ✅ **容易理解**：每次移動一個節點
- ✅ **邊界處理完善**：NULL 和單節點
- ✅ **優化 k**：使用 k % n 避免多餘旋轉
- ✅ **空間 O(1)**：不使用額外空間

**劣勢**：
- ⚠️ **時間複雜度高**：O(n²) 最壞情況
- ⚠️ **重複遍歷**：每次旋轉都要找倒數第二個節點

---

## 🌟 方法二：一次定位（最優解）

### 核心思想

```
不需要一次次移動！
可以一次性找到斷開點

關鍵觀察：
  右移 k 次 = 把後 k 個節點移到前面
  
  等價於：
  在第 (n-k) 個節點處斷開
  然後重新連接

範例：[1,2,3,4,5], k=2
  
  n = 5, k = 2
  斷開點 = n - k = 3
  
  在第 3 個節點後斷開：
    1 → 2 → 3 | 4 → 5
    
  重新連接：
    4 → 5 → 1 → 2 → 3 ✓
```

### 優化思路

```
步驟：
1. 計算長度 n
2. k = k % n
3. 如果 k == 0，不需要旋轉
4. 找到第 (n-k) 個節點（新的尾節點）
5. 連接：尾節點.next = head
6. 斷開：新尾.next = NULL
7. 更新 head
```

### 完整代碼實現

```c
struct ListNode* rotateRight(struct ListNode* head, int k) {
    // 邊界處理
    if (head == NULL || head->next == NULL || k == 0) {
        return head;
    }
    
    // 步驟 1：計算長度並找到尾節點
    struct ListNode* tail = head;
    int n = 1;
    while (tail->next != NULL) {
        tail = tail->next;
        n++;
    }
    
    // 步驟 2：優化 k
    k = k % n;
    if (k == 0) {
        return head;  // 不需要旋轉
    }
    
    // 步驟 3：找到新的尾節點（第 n-k 個節點）
    struct ListNode* newTail = head;
    for (int i = 1; i < n - k; i++) {
        newTail = newTail->next;
    }
    
    // 步驟 4：新的頭節點
    struct ListNode* newHead = newTail->next;
    
    // 步驟 5：重新連接
    tail->next = head;      // 原尾連接原頭
    newTail->next = NULL;   // 新尾斷開
    
    return newHead;
}
```

### 執行流程（最優解）

```
範例：[1,2,3,4,5], k = 2

═══════════════════════════════════════
步驟 1：計算長度並找尾節點

  遍歷鏈表：
    1 → 2 → 3 → 4 → 5
                    ↑
                   tail
  
  n = 5

═══════════════════════════════════════
步驟 2：優化 k

  k = k % n = 2 % 5 = 2
  k != 0，需要旋轉

═══════════════════════════════════════
步驟 3：找新尾節點

  新尾位置 = n - k = 5 - 2 = 3
  
  從 head 走 (n-k-1) = 2 步：
    head → 1 → 2 → 3
                    ↑
                 newTail

═══════════════════════════════════════
步驟 4：確定新頭節點

  newHead = newTail.next = 4
  
  1 → 2 → 3 → 4 → 5
              ↑
           newHead

═══════════════════════════════════════
步驟 5：重新連接

  操作 1：tail.next = head
    5 → 1
    
  操作 2：newTail.next = NULL
    3 → NULL
    
  結果：
    4 → 5 → 1 → 2 → 3 → NULL
    ↑
  newHead

═══════════════════════════════════════
返回：4 → 5 → 1 → 2 → 3 ✓
```

### 方法二的另一種實現（成環法）

```c
struct ListNode* rotateRight(struct ListNode* head, int k) {
    if (head == NULL || head->next == NULL) {
        return head;
    }
    
    // 步驟 1：計算長度並成環
    struct ListNode* tail = head;
    int n = 1;
    while (tail->next != NULL) {
        tail = tail->next;
        n++;
    }
    tail->next = head;  // 成環 ⭐
    
    // 步驟 2：優化 k
    k = k % n;
    if (k == 0) {
        tail->next = NULL;  // 解環
        return head;
    }
    
    // 步驟 3：找新尾節點
    int steps = n - k;
    struct ListNode* newTail = head;
    for (int i = 1; i < steps; i++) {
        newTail = newTail->next;
    }
    
    // 步驟 4：斷開環
    struct ListNode* newHead = newTail->next;
    newTail->next = NULL;
    
    return newHead;
}
```

### 成環法圖解

```
範例：[1,2,3,4,5], k = 2

步驟 1：成環
  1 → 2 → 3 → 4 → 5
  ↑________________↑
  
步驟 2：找到斷開點（第 n-k 個）
  1 → 2 → 3 → 4 → 5
          ↑   ↑
       newTail newHead
       
步驟 3：斷開
  4 → 5 → 1 → 2 → 3 → NULL ✓
```

---

## 🔄 兩種方法對比

| 比較項目 | 方法一（你的解法） | 方法二（最優解）⭐ |
|---------|------------------|------------------|
| **時間複雜度** | O(n * k) → O(n²) | O(n) ✓ |
| **空間複雜度** | O(1) | O(1) |
| **遍歷次數** | k 次（每次 O(n)） | 1-2 次 ✓ |
| **代碼複雜度** | 簡單 ⭐ | 中等 |
| **易理解性** | 容易 ⭐ | 需要思考 |
| **實際效率** | 較慢 | 快 ⭐⭐⭐ |
| **面試推薦** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

### 效率對比

```
測試：n = 1000, k = 999

方法一（你的解法）：
  遍歷次數：999 次
  每次遍歷：~1000 個節點
  總操作：~999,000 次

方法二（最優解）：
  遍歷次數：2 次
  第 1 次：計算長度（1000 個節點）
  第 2 次：找新尾（1 個節點）
  總操作：~1001 次
  
效率提升：~1000 倍！⭐
```

---

## 🎬 完整範例追蹤

### 範例 1：[1,2,3,4,5], k = 2

#### 使用你的方法：

```
初始：1 → 2 → 3 → 4 → 5

計算：n = 5, count = 2

─────────────────────────────────────
旋轉 1：
  找倒數第二：start = 4
  last = 5
  操作：5 → 1, 4 → NULL
  結果：5 → 1 → 2 → 3 → 4

旋轉 2：
  找倒數第二：start = 3
  last = 4
  操作：4 → 5, 3 → NULL
  結果：4 → 5 → 1 → 2 → 3 ✓

總遍歷：2 次完整遍歷
```

#### 使用最優方法：

```
初始：1 → 2 → 3 → 4 → 5

計算：n = 5, k = 2

找新尾：n - k = 3
  newTail = 3
  newHead = 4

連接：
  5 → 1
  3 → NULL

結果：4 → 5 → 1 → 2 → 3 ✓

總遍歷：1.5 次（計算長度 + 找新尾）
```

### 範例 2：[0,1,2], k = 4

```
初始：0 → 1 → 2

計算：n = 3, k = 4 % 3 = 1

═══════════════════════════════════════
方法一（你的解法）：

旋轉 1 次：
  找倒數第二：start = 1
  last = 2
  操作：2 → 0, 1 → NULL
  結果：2 → 0 → 1 ✓

═══════════════════════════════════════
方法二（最優解）：

找新尾：n - k = 3 - 1 = 2
  newTail = 1 (第 2 個節點)
  newHead = 2

連接：
  2 → 0
  1 → NULL

結果：2 → 0 → 1 ✓
```

### 範例 3：k = 0 或 k % n = 0

```
輸入：[1,2,3], k = 3

計算：k % n = 3 % 3 = 0

不需要旋轉！
直接返回 head ✓
```

---

## 📊 複雜度分析對比

### 時間複雜度詳細分析

```
方法一（你的解法）：
  計算長度：O(n)
  旋轉 k 次：
    每次找倒數第二：O(n)
    總共：O(k * n)
  
  優化後：k = k % n
    最壞：O(n * (n-1)) = O(n²)
    平均：O(n * n/2) = O(n²)
    最好：O(n)（k=1 時）

方法二（最優解）：
  計算長度：O(n)
  找新尾節點：O(n-k)
  總共：O(n) ✓
  
  所有情況都是 O(n) ⭐
```

### 實際運行時間對比

```
n = 100, k = 50:
  方法一：~5000 次操作
  方法二：~150 次操作
  快 33 倍

n = 1000, k = 500:
  方法一：~500,000 次操作
  方法二：~1500 次操作
  快 333 倍

n = 5000, k = 2500:
  方法一：~12,500,000 次操作
  方法二：~7500 次操作
  快 1666 倍！⭐
```

---

## 🧪 測試案例

### 基本測試

```c
// Test 1: 基本旋轉
head = [1,2,3,4,5], k = 2
→ [4,5,1,2,3] ✓

// Test 2: k > n
head = [0,1,2], k = 4
→ [2,0,1] ✓

// Test 3: k = 0
head = [1,2,3], k = 0
→ [1,2,3] ✓

// Test 4: k = n
head = [1,2,3], k = 3
→ [1,2,3] ✓
```

### 邊界測試

```c
// Test 5: 空鏈表
head = [], k = 0
→ [] ✓

// Test 6: 單節點
head = [1], k = 99
→ [1] ✓

// Test 7: 兩個節點
head = [1,2], k = 1
→ [2,1] ✓

// Test 8: 大 k 值
head = [1,2], k = 2000000000
→ [1,2] ✓ (k % 2 = 0)
```

### 效能測試

```c
// Test 9: 長鏈表
head = [1,2,3,...,1000], k = 999
方法一：~999,000 次操作
方法二：~1999 次操作 ⭐

// Test 10: k = n-1（最壞情況）
head = [1,2,3,...,100], k = 99
方法一：~9900 次操作
方法二：~199 次操作 ⭐
```

---

## ⚠️ 常見錯誤

### 錯誤 1：忘記優化 k

```c
// ❌ 錯誤：直接使用 k
while (k--) {  // k 可能很大！
    // 旋轉操作
}

// 問題：
// k = 2,000,000,000, n = 5
// 會執行 20 億次！💀

// ✓ 正確
k = k % n;  // 只需要旋轉 k % n 次
if (k == 0) return head;
```

### 錯誤 2：沒有處理 k = 0

```c
// ❌ 錯誤
k = k % n;
// 如果 k = 0，還繼續處理

// ✓ 正確
k = k % n;
if (k == 0) return head;  // 提前返回
```

### 錯誤 3：計算新尾位置錯誤

```c
// ❌ 錯誤：認為是第 k 個
struct ListNode* newTail = head;
for (int i = 0; i < k; i++) {  // 錯！
    newTail = newTail->next;
}

// ✓ 正確：是第 (n-k) 個
for (int i = 1; i < n - k; i++) {
    newTail = newTail->next;
}
```

### 錯誤 4：連接順序錯誤

```c
// ❌ 錯誤順序
newTail->next = NULL;   // 先斷開
tail->next = head;      // 再連接
// 問題：head 已經丟失！

// ✓ 正確順序
tail->next = head;      // 先連接
newTail->next = NULL;   // 再斷開
```

### 錯誤 5：忘記更新 head

```c
// ❌ 錯誤
struct ListNode* newHead = newTail->next;
// ... 操作
return head;  // 錯！應該返回 newHead

// ✓ 正確
return newHead;
```

### 錯誤 6：邊界條件

```c
// ❌ 錯誤：沒檢查空鏈表
if (head->next == NULL) return head;  // head 可能為 NULL

// ✓ 正確
if (head == NULL || head->next == NULL) return head;
```

---

## 💡 優化技巧深入理解

### 為什麼 k % n？

```
旋轉的週期性：

長度 n = 5 的鏈表：
  旋轉 0 次：[1,2,3,4,5]
  旋轉 1 次：[5,1,2,3,4]
  旋轉 2 次：[4,5,1,2,3]
  旋轉 3 次：[3,4,5,1,2]
  旋轉 4 次：[2,3,4,5,1]
  旋轉 5 次：[1,2,3,4,5] ← 回到原點
  旋轉 6 次：[5,1,2,3,4] ← 和旋轉1次相同
  
結論：
  旋轉 k 次 = 旋轉 (k % n) 次
  
數學證明：
  位置 i 的節點
  旋轉 k 次後在位置 (i + k) % n
  和旋轉 (k % n) 次的結果相同
```

### 為什麼斷開點在第 (n-k) 個？

```
思考過程：

右移 k 次 = 後 k 個移到前面

範例：[1,2,3,4,5], k=2
  
  後 2 個：[4,5]
  前 3 個：[1,2,3]
  
  結果：[4,5] + [1,2,3]
  
斷開點：
  在「前 3 個」的最後
  即第 3 個 = n - k = 5 - 2 = 3 ✓

通用公式：
  前半部分：前 (n-k) 個
  後半部分：後 k 個
  斷開點：第 (n-k) 個節點之後
```

### 為什麼方法二快這麼多？

```
關鍵差異：遍歷次數

方法一：
  旋轉 k 次
  每次都要找倒數第二個節點
  = k 次完整遍歷
  
方法二：
  只需要找一次新尾節點
  = 1 次部分遍歷

比喻：
  方法一：搬家 k 次，每次搬 1 件家具
  方法二：一次性把要搬的都標記好，一起搬
  
效率差距：O(n²) vs O(n)
```

---

## 🔗 與其他題目的聯繫

### 189. Rotate Array

```c
// 189：旋轉數組
輸入：nums = [1,2,3,4,5,6,7], k = 3
輸出：[5,6,7,1,2,3,4]

// 相同思想：
// 1. k = k % n
// 2. 反轉三次
//    - 反轉全部
//    - 反轉前 k 個
//    - 反轉後 n-k 個

// 與本題的聯繫：
// 都是旋轉操作
// 數組可以反轉，鏈表需要重新連接
```

### 206. Reverse Linked List

```c
// 206：反轉整個鏈表
// 基礎技能，本題會用到

// 如果旋轉 k = n：
// rotateRight(head, n) = head
// 相當於不動

// 如果旋轉 k = n/2：
// 前後對調
```

### 24. Swap Nodes in Pairs

```c
// 24：兩兩交換節點
範例：[1,2,3,4] → [2,1,4,3]

// 可以看作特殊的旋轉：
// 局部旋轉 k=1，範圍是每 2 個節點
```

---

## 📚 延伸練習

### 相關題目

| 題號 | 題目 | 難度 | 關鍵技巧 |
|------|------|------|---------|
| 61 | Rotate List | Medium | 計算位置 + 重連 ⭐ |
| 189 | Rotate Array | Medium | 數組旋轉 |
| 206 | Reverse Linked List | Easy | 鏈表反轉 |
| 19 | Remove Nth Node From End | Medium | 雙指針 |
| 143 | Reorder List | Medium | 快慢指針 + 反轉 |
| 25 | Reverse Nodes in k-Group | Hard | 分組反轉 |

### 進階思考

#### 問題 1：如果要左旋轉怎麼辦？

```c
// 左旋轉 k 次 = 右旋轉 (n-k) 次

範例：[1,2,3,4,5]
  左旋 2 次：[3,4,5,1,2]
  右旋 3 次：[3,4,5,1,2]
  
實現：
  rotateLeft(head, k) {
      return rotateRight(head, n - k);
  }
```

#### 問題 2：如何實現循環右移？

```c
// 就是本題！
// 右移 = 循環右移

// 區別於「刪除右邊 k 個」：
// 旋轉：把右邊移到左邊
// 刪除：直接去掉
```

#### 問題 3：如何旋轉雙向鏈表？

```c
// 雙向鏈表旋轉更簡單：
// 1. 成環（頭尾互連）
// 2. 找新頭
// 3. 斷開

// 優勢：可以從尾部往回走
// 找第 (n-k) 個 = 從尾部走 k 步
```

#### 問題 4：如何在 O(1) 時間旋轉？

```c
// 不可能！
// 至少要 O(n) 計算長度
// 除非預先知道長度和尾節點

// 但可以優化：
// 如果多次旋轉同一個鏈表
// 可以預計算並保存 n 和 tail
```

---

## 🎓 總結

**61. Rotate List 的核心**：

```
問題：旋轉鏈表（右移 k 位）
關鍵：找到斷開點並重新連接

方法一（你的解法）：
  - 逐次旋轉，每次移動最後節點
  - 時間 O(n * k) → O(n²)
  - 邏輯清晰，易理解 ✓
  
方法二（最優解）：
  - 一次性定位，找到新尾節點
  - 時間 O(n) ✓
  - 效率最優 ⭐
```

**關鍵記憶點**：
1. ✅ **優化 k**：k = k % n（必須！）
2. ✅ **k = 0 判斷**：提前返回
3. ✅ **斷開點位置**：第 (n-k) 個節點
4. ✅ **連接順序**：先連後斷
5. ✅ **新頭節點**：newTail->next
6. ✅ **邊界處理**：空鏈表、單節點

**你的解法分析**：
```
優點：
✓ 功能正確
✓ 邏輯清晰
✓ 優化了 k（k % n）
✓ 邊界處理完善
✓ 空間 O(1)

可以改進：
⚠️  時間複雜度 O(n²)
⚠️  重複遍歷 k 次

改進方向：
→ 使用方法二
→ 一次定位，O(n) 時間
→ 效率提升 100-1000 倍

面試評價：⭐⭐⭐⭐
你的解法是正確的！
對於小規模測試完全可以
但如果面試官問「能否更快」
展示方法二會加分
```

**面試要點**：
- 先寫你的解法（快速通過）⭐⭐⭐
- 面試官問優化時展示方法二 ⭐⭐⭐⭐⭐
- 能解釋 k % n 的必要性 ⭐⭐⭐⭐
- 理解為什麼斷開點在 (n-k) ⭐⭐⭐⭐
- 能分析兩種方法的複雜度 ⭐⭐⭐

**記憶口訣**：
```
「模長定尾重連接」

模：k = k % n（模運算優化）
長：計算鏈表長度 n
定尾：找到新尾節點（第 n-k 個）
重連接：先連後斷

公式：
  新尾 = 第 (n-k) 個節點
  新頭 = 新尾的下一個
```

**兩種方法的選擇**：
```
你的方法（逐次旋轉）：
  ✓ 邏輯直觀
  ✓ 容易想到
  ⚠️  效率較低（O(n²)）
  
  適合：
  - 快速實現
  - 小規模數據
  - 展示思路

最優方法（一次定位）：
  ✓ 效率最高（O(n)）
  ✓ 只遍歷 1-2 次
  ⚠️  需要思考
  
  適合：
  - 大規模數據
  - 追求最優
  - 面試加分

建議：兩種都掌握
```

**與 Firmware 面試的關聯**：
- 循環數據結構的處理 ⭐⭐⭐⭐
- 優化重複操作（k % n）⭐⭐⭐⭐⭐
- 時間複雜度優化意識 ⭐⭐⭐⭐
- 邊界條件處理（k=0, k>n）

你的解法是正確的！在 LeetCode 上能通過。但掌握方法二會讓你在面試中更有競爭力，特別是當面試官問「能否優化」時 💪

建議：
1. 你的方法保留（作為第一反應）
2. 學習方法二（作為優化方案）
3. 理解為什麼快 100-1000 倍
4. 能清楚解釋兩種方法的權衡

這道題很好地展示了「正確」vs「最優」的區別！👍
